# Ensure that Nginx serves files from the public directory and passes requests to Laravel's index.php
try_files $uri $uri/ /index.php?$query_string;

# Handle Authorization Header (to ensure Authorization is passed to Laravel)
if ($http_authorization) {
    set $auth_header $http_authorization;
}

# Redirect Trailing Slashes If Not A Folder...
if (!-d $request_filename) {
    rewrite ^/(.+)/$ /$1 permanent;
}

# Cache static files for performance improvement
location ~* \.(?:css|js|jpg|jpeg|png|gif|ico|svg|webp)$ {
    expires max;
    access_log off;
}
